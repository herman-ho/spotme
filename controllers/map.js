const express = require('require');

module.exports = {
    registerRouters() {
        const router = express.Router().

        router.get('/', function(req, res) {
            res.('maps');
            
        return map;
    },
};